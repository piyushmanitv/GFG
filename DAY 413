Max sum in the configuration
Difficulty: MediumAccuracy: 36.56%Submissions: 124K+Points: 4Average Time: 30m
Given an integer array arr[]. Find the maximum value of the sum of i*arr[i] for all 0 ≤ i ≤ arr.size()-1. 
The only operation allowed is to rotate(clockwise or counterclockwise) the array any number of times.








class Solution {
  public:
    int maxSum(vector<int> &arr) {
        // code here
        int n = arr.size();
        int ans = 0;
        int currSum = 0;
        for(int i=0;i<n;i++){
            currSum+=(arr[i]*i);
        }
        ans = max(ans, currSum);
        int total = 0;
        for(int i=0;i<n;i++) total+=arr[i];
        for(int i=0;i<n-1;i++){
            currSum+=((arr[i]*n)-total);
            ans = max(ans, currSum);
        }
        return ans;
    }
};
