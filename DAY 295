Nearly sorted
Difficulty: MediumAccuracy: 75.25%Submissions: 81K+Points: 4Average Time: 30m
Given an array arr[], where each element is at most k positions away from its correct position in the sorted order.
Your task is to restore the sorted order of arr[] by rearranging the elements in place.

Note: Don't use any sort() method.











class Solution {
public:
    virtual void nearlySorted(vector<int> &a, int k){
      priority_queue<int, vector<int>, greater<int>> pq;
      int i=0;
      for(auto x: a){
          pq.push(x);
          if(pq.size() > k){
              a[i] = pq.top();
              pq.pop();
              i++;
          }
      }
      while(!pq.empty()){
          a[i] = pq.top();
          pq.pop();
          i++;
      }
      return;           
    }
};
