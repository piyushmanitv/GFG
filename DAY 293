Number of BST From Array
Difficulty: HardAccuracy: 88.36%Submissions: 1K+Points: 8
You are given an integer array arr[] containing distinct elements.

Your task is to return an array where the ith element denotes the number of unique BSTs formed when arr[i] is chosen as the root.











class Solution {
  public:
    vector<int> bsts = {1, 1, 2, 5, 14, 42 };
    vector<int> countBSTs(vector<int>& arr) {
        // Code here
        int n=arr.size();
        vector<int> bstCount;
        for(int i = 0; i<n; i++)
        {
            int l=0, g=0;
            for(int j = 0;j<n;j++)
            {
                if(arr[i]<arr[j])
                {
                    g++;
                }
                else if(arr[i]>arr[j])
                {
                    l++;
                }
            }
            bstCount.push_back(bsts[l]*bsts[g]);
        }
        return bstCount;
    }
};

